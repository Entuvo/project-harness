# Patches — upstream edit ledger
<!-- harness:patches v1 · ZERO data rows is the designed default. Every ALLOW_PROTECTED_EDIT bypass lands a row in the SAME commit. A patch outliving its retirement condition is a silent re-fork — audits evaluate every condition. -->

| Date | Path patched | Why (seam workaround attempted first?) | Upstream issue/PR | Retirement condition (machine-evaluable) |
|---|---|---|---|---|
